import React, { Component, PropTypes } from 'react';

export default class Candidate extends Component {
    static propTypes = {

    }

    render() {
        return (
            <div>
            </div>
        );
    }
}
